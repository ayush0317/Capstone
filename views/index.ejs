<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Name</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <!-- ✅ Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Restaurant Name</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/menu">Menu</a></li>
                    <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
                    <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
                    <li class="nav-item">
                        <a class="btn btn-danger ms-3" href="/book">Book a Table</a>
                    </li>
                    <li class="nav-item ms-3">
                        <a class="btn btn-outline-light" href="/login">Sign In</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="btn btn-success" href="/signup">Sign Up</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- ✅ Display Login Success Message -->
    <% if (message && message.trim() !== "") { %>
        <div class="alert alert-success text-center mt-3" role="alert">
            <%= message %>
        </div>
    <% } %>
    
    <!-- ✅ Hero Section -->
    <div class="container-fluid text-center bg-light py-5">
        <h1 class="display-4">Experience Fine Dining</h1>
        <p class="lead">Indulge in a unique culinary journey at Restaurant Name.</p>
        <a href="/book" class="btn btn-danger btn-lg">Book a Table</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
